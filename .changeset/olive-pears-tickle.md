---
"postcss-variable-theming": minor
---

Add a `propAlias` option to name variables independently of the property name, along with `tailwindPropAlias` and `groupedPropAlias` presets exported from `postcss-variable-theming/preset`
